machine:
  node:
    version: 6.9.1
  environment:
    GCLOUD_PROJECT: bus-eta-bot
test:
  override:
    - npm run nyc --silent
    - npm run coverage
  post:
    - bash <(curl -s https://codecov.io/bash)
